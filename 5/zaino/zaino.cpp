#include <fstream>
#include <iostream>
#include <vector>

using namespace std;

int main(){
  ifstream in("input.txt");
  ofstream out("output.txt");

  int n,c;
  in >> n >> c;

  vector<vector<int>> dp(n);
  for (int i = 0; i < n; i++) {
    dp[i]
  }

  return 0;
}
